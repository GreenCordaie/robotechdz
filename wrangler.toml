name = "100-pc-ia"
compatibility_date = "2024-04-03"
compatibility_flags = ["nodejs_compat"]


[vars]
# DATABASE_URL est géré via les secrets Cloudflare (wrangler secret put)
# UPSTASH_REDIS_REST_URL est géré via les secrets
# UPSTASH_REDIS_REST_TOKEN est géré via les secrets

[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "robotech-uploads"
